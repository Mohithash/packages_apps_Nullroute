<?xml version="1.0" encoding="utf-8"?>
<!--
    One key/value row, shared by the Update log and Diagnostics. The value is
    monospaced and selectable: these are the strings a user will be asked to read
    back or paste into a bug report, so they have to be exact and copyable.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <TextView
        android:id="@+id/row_title"
        style="@style/Nullroute.Label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/row_detail"
        style="@style/Nullroute.Body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textIsSelectable="true" />
</LinearLayout>
